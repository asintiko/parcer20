v1.3.26

Исправления:
- Telegram-источники в Транзакциях теперь корректно показывают конкретный бот/группу, включая supergroup с отрицательным chat_id, например UBpay_demo.
- Фильтр по конкретному Telegram-источнику работает по source_chat_id.
- Колонка `№ опер.` добавлена и стабильно вставляется после `№` даже при старом сохранённом состоянии таблицы.
- Колонка `№ опер.` принудительно видима, чтобы не теряться из-за legacy localStorage.
- QR-вход для Telegram ID `1957390574` теперь всегда приводит к admin-аккаунту.
- Завершение сессий исправлено: текущая сессия определяется по актуальному JWT, без зависания на первом рендере.

Сервер:
- backend hotfix применён без миграций и без изменений схемы БД.
