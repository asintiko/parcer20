# PDF Receipt Bot 🧾

Telegram бот для извлечения текстовых данных из PDF чеков (Uzum Bank и другие банки Узбекистана).

## Возможности

- 📄 Извлечение текста из PDF чеков
- 🔍 OCR для сканированных чеков
- 📊 Структурированный вывод данных (карты, суммы, даты)
- 🏦 Поддержка чеков Uzum Bank

## Установка

### 1. Системные зависимости

**macOS:**
```bash
brew install poppler tesseract tesseract-lang
```

**Ubuntu/Debian:**
```bash
sudo apt-get install poppler-utils tesseract-ocr tesseract-ocr-rus
```

### 2. Python зависимости

```bash
cd pdf_receipt_bot
python -m venv venv
source venv/bin/activate  # macOS/Linux
pip install -r requirements.txt
```

### 3. Настройка

```bash
cp .env.example .env
# Отредактируйте .env и добавьте ваш PDF_BOT_TOKEN
```

Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram.

## Запуск

```bash
python bot.py
```

## Использование

1. Запустите бота командой `/start`
2. Отправьте PDF файл чека
3. Получите извлеченные данные

## Структура извлекаемых данных

Для чеков Uzum Bank:
- Карта отправителя / получателя
- Имя отправителя / получателя
- ID транзакции
- Дата транзакции / получения
- Комиссии
- Суммы перевода
